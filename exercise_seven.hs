-- variable names must start with lowerase letter --> N
-- use backquote (`) instead of normal quote (')
-- correct intendation after 'where'
n = a `div` length xs
    where
        a = 10
        xs = [1,2,3,4,5]